<header class="p-4 flex gap-2 justify-center items-center">
  <h1 class="text-3xl font-bold text-center">Edit User</h1>
  <a class="inline-block h-max rounded-md px-3 py-2 text-md font-bold text-white bg-red-700 hover:bg-red-800 transition-colors" href="/admin/users">Cancel</a>
</header>

<div class="flex justify-center items-center">
  <form class="w-5/12 border-4 border-black p-4 flex flex-col gap-2 bg-yellow-200" action="/admin/users/{{ user.id }}/edit" method="POST">
    <div class="flex flex-col gap-2">
      <p><strong>Username: </strong>{{ user.username }}</p>
      <p><strong>Email: </strong>{{ user.email }}</p>
    </div>
    <div class="flex gap-2 items-center">
      <label class="font-bold">Active:</label>
      <input class="w-4 h-4" type="checkbox" name="active" {{#if user.active}} checked {{/if}}>
    </div>
    <div class="flex gap-2 items-center">
      <label class="font-bold" for="birthday">Birthday:</label>
      <input id="birthday" class="border border-black rounded p-2 bg-gray-200" type="date" name="birthday" value="{{ parseDate user.birthday }}">
    </div>
    <input class="rounded p-2 font-bold bg-yellow-400 hover:bg-yellow-500 cursor-pointer transition-colors" type="submit" value="Edit">
  </form>
</div>